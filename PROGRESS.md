# Progress

- 2026-05-07 Checkpoint 1: Created `backend/ShopApi` ASP.NET Core Web API project and verified `dotnet build backend/ShopApi` passed with 0 warnings and 0 errors. Evidence: local build output; commit pending.
- 2026-05-07 Checkpoint 2: Added Pomelo EF Core MySQL provider, created `ShopDbContext`, configured `DefaultConnection`, and verified `dotnet build backend/ShopApi` passed with 0 warnings and 0 errors. Evidence: local build output; commit pending.
- 2026-05-07 Checkpoint 3: Added Code First models for users, categories, products, cart items, orders, and order items; configured DbSets, indexes, precision, check constraints, and relationships in `ShopDbContext`; verified `dotnet build backend/ShopApi` passed with 0 warnings and 0 errors. Evidence: local build output; commit pending.
- 2026-05-07 Checkpoint 3.5: Documented Android model to backend entity/DTO mapping, including immediate scope, phase 2 models, and field mismatches to handle during Retrofit integration. Evidence: docs update only; commit pending.
- 2026-05-07 Checkpoint 4: Added EF Core design tooling, created `InitialCreate` migration, updated local MySQL `shop_api`, and verified admin seed command reports `admin@shop.local` exists. Evidence: `dotnet build backend/ShopApi`, `dotnet ef database update`, and `dotnet run --no-launch-profile --project backend/ShopApi -- --seed-admin`; commit pending.
- 2026-05-07 Checkpoint 5: Added JWT auth configuration, auth DTOs, and `AuthController` register/login/me endpoints; verified `dotnet build backend/ShopApi` passed and an end-to-end local auth test returned `AUTH_TEST_OK` without password fields in responses. Evidence: local build and HTTP test; commit pending.
- 2026-05-07 Checkpoint 6: Added category DTOs and `CategoriesController` with public GET plus admin-only create/update/delete; verified `dotnet build backend/ShopApi` passed and local HTTP test returned `CATEGORY_TEST_OK` with user create forbidden. Evidence: local build and HTTP test; commit pending.
- 2026-05-07 Review cleanup: Simplified backend by removing the template weather endpoint, making `Jwt:Key` fail fast at startup, and rejecting whitespace-only usernames/category names; verified `dotnet build backend/ShopApi` passed and local smoke test returned `SMOKE_TEST_OK`. Evidence: local build and HTTP smoke test; commit pending.
- 2026-05-07 Checkpoint 7: Added product DTOs and `ProductsController` with public product listing/detail, admin create/update/delete, and simple image upload to `wwwroot/uploads/products`; verified `dotnet build backend/ShopApi` passed and local HTTP smoke test returned `PRODUCT_SMOKE_OK` with admin CRUD, upload, static image access, and user create forbidden. Evidence: local build and HTTP smoke test; commit pending.
