# Progress

- 2026-05-07 Checkpoint 1: Created `backend/ShopApi` ASP.NET Core Web API project and verified `dotnet build backend/ShopApi` passed with 0 warnings and 0 errors. Evidence: local build output; later committed/pushed.
- 2026-05-07 Checkpoint 2: Added Pomelo EF Core MySQL provider, created `ShopDbContext`, configured `DefaultConnection`, and verified `dotnet build backend/ShopApi` passed with 0 warnings and 0 errors. Evidence: local build output; later committed/pushed.
- 2026-05-07 Checkpoint 3: Added Code First models for users, categories, products, cart items, orders, and order items; configured DbSets, indexes, precision, check constraints, and relationships in `ShopDbContext`; verified `dotnet build backend/ShopApi` passed with 0 warnings and 0 errors. Evidence: local build output; later committed/pushed.
- 2026-05-07 Checkpoint 3.5: Documented Android model to backend entity/DTO mapping, including immediate scope, phase 2 models, and field mismatches to handle during Retrofit integration. Evidence: docs update only; later committed/pushed.
- 2026-05-07 Checkpoint 4: Added EF Core design tooling, created `InitialCreate` migration, updated local MySQL `shop_api`, and verified admin seed command reports `admin@shop.local` exists. Evidence: `dotnet build backend/ShopApi`, `dotnet ef database update`, and `dotnet run --no-launch-profile --project backend/ShopApi -- --seed-admin`; later committed/pushed.
- 2026-05-07 Checkpoint 5: Added JWT auth configuration, auth DTOs, and `AuthController` register/login/me endpoints; verified `dotnet build backend/ShopApi` passed and an end-to-end local auth test returned `AUTH_TEST_OK` without password fields in responses. Evidence: local build and HTTP test; later committed/pushed.
- 2026-05-07 Checkpoint 6: Added category DTOs and `CategoriesController` with public GET plus admin-only create/update/delete; verified `dotnet build backend/ShopApi` passed and local HTTP test returned `CATEGORY_TEST_OK` with user create forbidden. Evidence: local build and HTTP test; later committed/pushed.
- 2026-05-07 Review cleanup: Simplified backend by removing the template weather endpoint, making `Jwt:Key` fail fast at startup, and rejecting whitespace-only usernames/category names; verified `dotnet build backend/ShopApi` passed and local smoke test returned `SMOKE_TEST_OK`. Evidence: local build and HTTP smoke test; later committed/pushed.
- 2026-05-07 Checkpoint 7: Added product DTOs and `ProductsController` with public product listing/detail, admin create/update/delete, and simple image upload to `wwwroot/uploads/products`; verified `dotnet build backend/ShopApi` passed and local HTTP smoke test returned `PRODUCT_SMOKE_OK` with admin CRUD, upload, static image access, and user create forbidden. Evidence: local build and HTTP smoke test; later committed/pushed.
- 2026-05-07 Checkpoint 8: Added cart DTOs and `CartController` with JWT-only cart read, add, update, delete item, and clear cart endpoints; verified `dotnet build backend/ShopApi` passed and local HTTP smoke test returned `CART_SMOKE_OK` with duplicate product merge and cross-user item protection. Evidence: local build and HTTP smoke test; later committed/pushed.
- 2026-05-07 Checkpoint 9: Added order DTOs and `OrdersController` with create order from cart, my orders, admin all orders, and admin status update; verified `dotnet build backend/ShopApi` passed and local HTTP smoke test returned `ORDER_SMOKE_OK` with empty-cart rejection, insufficient-stock rejection, cart clearing, stock reduction, and role protection. Evidence: local build and HTTP smoke test; later committed/pushed.
- 2026-05-07 Checkpoint 10: Verified backend before Android integration; `dotnet build backend/ShopApi` passed, `dotnet list backend/ShopApi package --vulnerable --include-transitive` found no vulnerable packages, and local HTTP smoke test returned `BACKEND_VERIFY_OK` for auth, category, product upload/listing, cart, order, admin order listing, and admin status update. Evidence: local build, NuGet vulnerability check, and HTTP smoke test; later committed/pushed.
- 2026-05-07 Checkpoint 11: Connected Android Auth and Product flows to backend REST API with Retrofit/OkHttp, Internet permission, emulator base URL `10.0.2.2:5053`, remote DTOs, `AuthApi`, `ProductApi`, and repositories no longer using Room for Auth/Product reads; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` built successfully. Evidence: Android debug build; later committed/pushed.
- 2026-05-07 Checkpoint 12: Connected Android Cart flow to backend REST API with `CartApi`, cart DTOs, and `CartRepository` backed by authenticated API calls for load, add, update quantity, delete item, and clear cart; updated cart/order view models to refresh cart from backend; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` built successfully. Evidence: Android debug build; later committed/pushed.
- 2026-05-07 Checkpoint 13: Connected Android Order and Checkout flows to backend REST API with `OrderApi`, order DTOs, and `OrderRepository` backed by authenticated API calls for create order, my orders, admin orders, and admin status update; Checkout now navigates only after create order succeeds and refreshes cart after backend clears it; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` built successfully. Evidence: Android debug build; later committed/pushed.
- 2026-05-07 Checkpoint 14: Cleaned Android after REST migration by connecting Category to backend, removing unused Room DAOs for user/product/category/cart/order, reducing `AppDatabase` to Address only, removing stale `ApiService`/converters, and allowing product create/update to carry optional `imageUrl`; verified `dotnet build backend/ShopApi`, `./gradlew.bat :app:assembleDebug --no-daemon --console=plain`, and final backend HTTP E2E returned `FINAL_BACKEND_E2E_OK`. Evidence: backend build, Android debug build, final HTTP E2E; later committed/pushed.
- 2026-05-07 Room removal follow-up: Removed Room completely from Android by deleting `AddressDao` and `AppDatabase`, removing Room dependencies/KSP arguments, removing Room annotations from `Address`, and replacing `AddressRepository` with a simple in-memory repository; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` exited successfully. Evidence: Android debug build; later committed/pushed.
- 2026-05-10 Checkpoint 15: Synchronized documentation with the actual REST migration state: Room is fully removed, order DTO mapping uses API `id` plus ISO `orderDate`, backend image upload is distinguished from pending Android image upload, and the next checkpoints are documented for image display, admin product error handling, Android image upload, backend review, and final E2E. Evidence: docs diff review only.
- 2026-05-10 Checkpoint 15 docs refinement: Expanded the execution plan from Checkpoint 15 onward with concrete actions, expected file changes, non-goals, verification commands, and review/push criteria for checkpoints 15-20. Evidence: docs diff review only.
- 2026-05-10 Checkpoint docs review structure: Added `Vấn đề hiện tại` and `Mục tiêu thực hiện` sections to checkpoints 15-20 so each checkpoint is easier to review before execution. Evidence: docs diff review only.
- 2026-05-10 Checkpoint 16: Displayed backend product images in Android by adding Coil Compose, normalizing backend image URLs, passing `imageUrl` into product cards, and rendering product detail images with a simple fallback; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` built successfully. Evidence: Android debug build; warnings remain for local `sdk.dir` and existing deprecated ArrowBack icons.
- 2026-05-10 Checkpoint 17: Made Android admin product actions report real API success/failure by returning `Boolean` from product create/update/delete, refreshing product lists after successful mutations, and showing simple inline errors instead of navigating away on failed add/update/delete; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` built successfully. Evidence: Android debug build; warning remains for local `sdk.dir`.
- 2026-05-10 Checkpoint 18: Added Android product image upload by wiring Retrofit multipart `POST api/products/{id}/image`, reading selected image `Uri` into a multipart `file` part, adding simple image pickers to admin add/update product screens, uploading after create/update, and rolling back newly created products if image upload fails; verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain` built successfully. Evidence: Android debug build; warning remains for local `sdk.dir`.
- 2026-05-10 Checkpoint 19: Reviewed backend after Android image upload integration and confirmed the Auth, Category, Product, Cart, and Order endpoints still match the Android flows with simple controller-level logic, DTO validation, admin authorization, and no password fields in responses; no backend code changes were needed. Verified `dotnet build backend/ShopApi --nologo` passed with 0 warnings and 0 errors, and `dotnet list backend/ShopApi package --vulnerable --include-transitive` reported no vulnerable packages. Evidence: backend build, NuGet vulnerability check, and controller/DTO review.
- 2026-05-10 Checkpoint 20: Completed final verification for the REST API migration. Verified `dotnet build backend/ShopApi --nologo` passed with 0 warnings and 0 errors, `./gradlew.bat :app:assembleDebug --no-daemon --console=plain --stacktrace` built successfully, backend HTTP E2E returned `FINAL_CHECKPOINT_20_E2E_OK` for admin/user auth, category/product image upload, product listing, cart, checkout order, my orders, admin orders, and status update, and emulator runtime returned `FINAL_CHECKPOINT_20_EMULATOR_OK` after installing and launching `com.example.shop/.MainActivity`. Evidence: backend build, Android debug build, backend E2E smoke, and emulator install/launch smoke; warning remains for local `sdk.dir`.
- 2026-05-10 Dev login simplification: Made Development backend startup automatically prepare one fixed admin account, `admin@shop.local` / `Admin123456!`, and provide a local Development JWT key so local `dotnet run` is enough for login testing. Existing admin rows are reset to role `ADMIN` with the default password during Development startup. Verified `dotnet build backend/ShopApi --nologo` passed with 0 warnings and 0 errors, and direct API login returned `ADMIN_LOGIN_OK role=ADMIN`.
- 2026-05-10 Admin product image cleanup: Removed the old manual `Link hình ảnh (URL)` fields from Android admin add/update product screens so admins use only the local image picker added in Checkpoint 18. Product update keeps the existing backend image URL when no new image is selected. Verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain --stacktrace` built successfully.
- 2026-05-10 Checkpoint 21 planning/review: Reviewed Android models against backend entities/DTOs after Checkpoint 20, confirmed backend core models are complete for the shopping flow, reviewed `Address` as the only model with active UI but no backend persistence, marked `Payment`/`Review`/`Notification` as placeholder/future scope, and drafted follow-up completion checkpoints for user review. Evidence: docs diff review only; no code changed.
- 2026-05-10 Checkpoint 22 direction update: User chose the simplest address approach: no address book, no Address API, checkout keeps direct address/phone input and backend stores those fields on the order. Removed the Android address book route, profile entry, in-memory address repository, address view model, and address screens; updated the plan to replace Address API phases with address cleanup, model cleanup, placeholder cleanup, and final E2E. Verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain --stacktrace` passed by setting `GRADLE_OPTS=-Dorg.gradle.cache.internal.locklistener=false` to avoid a local Gradle file-lock port conflict.
- 2026-05-10 Checkpoint 23: Standardized Android model contract by changing `CartItem.productId` from `String` to `Int`, removing the old string/int conversion in cart DTO/repository/product detail, adding `imageUrl` to `OrderItem`, mapping backend `OrderItemResponse.imageUrl`, and cleaning stale database comments in cart/order/checkout screens. Updated the execution plan so the next checkpoint is placeholder cleanup. Verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain --stacktrace` passed with `GRADLE_OPTS=-Dorg.gradle.cache.internal.locklistener=false`.
- 2026-05-10 Checkpoint 24: Removed placeholder code that had no real route/API flow: payment model/repository/viewmodel/screens, review model/repository/viewmodel/screens, notification model/repository/viewmodel/screen, and Firebase auth/firestore stubs. Kept admin user management and implemented it as a minimal real feature: backend `UsersController` with admin-only list/delete endpoints, Android `UserApi`/`UserRepository`, admin dashboard entry, navigation route, `AdminUserViewModel`, and `ManageUserScreen`. Delete is limited to regular users and now removes that user's order items, orders, and cart items in one transaction before deleting the user. Verified `dotnet build backend/ShopApi --nologo` and `./gradlew.bat :app:assembleDebug --no-daemon --console=plain --stacktrace` passed.
- 2026-05-10 Cleanup: Removed unused onboarding flow (`RootNavGraph`, `BoardingScreen`, `BoardingViewModel`, and stale routes) and removed the local notification switch from settings so Android no longer exposes onboarding/review/notification placeholder features. Verified `./gradlew.bat :app:assembleDebug --no-daemon --console=plain --stacktrace` passed with `GRADLE_OPTS=-Dorg.gradle.cache.internal.locklistener=false`.
- 2026-05-10 Review cleanup: Fixed the cart bottom bar badge to use the real cart quantity instead of a hardcoded `3`, removed unused favorite/order-detail/congrats/profile placeholder screens and view models, simplified `MainActivity`/`MainScreen`, and replaced the stale `weatherforecast` HTTP sample with real auth/product/category/user requests. Verified `dotnet build backend/ShopApi --nologo`, `./gradlew.bat clean :app:assembleDebug --no-daemon --console=plain --stacktrace`, and `./gradlew.bat "-Dorg.gradle.cache.internal.locklistener=false" :app:testDebugUnitTest --no-daemon --console=plain --stacktrace` passed.
- 2026-05-10 Backend startup cleanup: Added automatic EF Core migration at startup before admin seeding so `dotnet run` applies pending MySQL migrations and prepares the development admin account when MySQL is already available. Verified `dotnet build backend/ShopApi --nologo` passed.
- 2026-05-19 Phase Google Sign-In planning: Added Section 17 to the migration plan with Checkpoints 26-29 covering Google Cloud OAuth Client ID setup, additive backend `/api/auth/google` endpoint that reuses the existing JWT and `CreateToken`, Android Credential Manager + googleid integration on the existing login screen, and E2E verification of new/linked/legacy accounts. The phase keeps the current email/password flow untouched and only adds a nullable `GoogleSub` column with a unique index on `User`. Evidence: docs diff review only; no code changed.
- 2026-05-19 Phase Google Sign-In planning: Added Section 17 to the migration plan with Checkpoints 26-29 covering Google Cloud OAuth Client ID setup, additive backend `/api/auth/google` endpoint that reuses the existing JWT and `CreateToken`, Android Credential Manager + googleid integration on the existing login screen, and E2E verification of new/linked/legacy accounts. The phase keeps the current email/password flow untouched and only adds a nullable `GoogleSub` column with a unique index on `User`. Evidence: docs diff review only; no code changed.
- 2026-05-21 Checkpoint 26: Prepared Google Cloud OAuth for Google Sign-In in project `shop-android` (`zippy-nexus-497009-c3`). Configured Google Auth Platform as External with non-sensitive scopes `openid`, `userinfo.email`, and `userinfo.profile`; added test user `locv2659@gmail.com`; created OAuth client `Shop Backend` (Web application) with Client ID `826757086511-es3htk7un7lq7lvlpmkqppmp3h7nnjd2.apps.googleusercontent.com`; created OAuth client `Shop Android Debug` (Android) for package `com.example.shop` with debug SHA-1 `33:DB:2C:01:F2:D8:E2:70:27:39:2F:EB:1B:5A:BF:8B:EA:A9:A2:86` and Client ID `826757086511-gn16lrjbrlu8ml1mtodmulv81qbi7o8v.apps.googleusercontent.com`. Added detailed setup notes in `docs/google-oauth-setup.md`. Evidence: Google Cloud Console verification; `keytool -list -v -keystore ~/.android/debug.keystore`; no code changed and no client secret recorded.
- 2026-05-21 Checkpoint 27: Added backend Google Sign-In endpoint `POST /api/auth/google` using `Google.Apis.Auth` to validate Google ID tokens against the Web Client ID, reject invalid or unverified-email tokens, create new Google users, link existing email/password users by `GoogleSub`, and return the existing app JWT response shape. Added nullable `Users.GoogleSub` with a unique index and applied the `AddGoogleAuth` EF migration to local MySQL. Evidence: installed system .NET SDK 9.0 via Fedora `dnf`; `dotnet build backend/ShopApi --nologo` passed with 0 warnings and 0 errors; `dotnet ef database update --project backend/ShopApi --no-build` applied `20260521173049_AddGoogleAuth`; local HTTP smoke returned 401 for an invalid Google token and 200 for existing admin login; secret scan confirmed no OAuth client secret in the diff.
- 2026-05-21 Checkpoint 28: Added Android Google Sign-In with minimal changes: Credential Manager/googleid dependencies, `GOOGLE_WEB_CLIENT_ID`, `GoogleLoginRequest`, Retrofit `POST /api/auth/google`, `AuthRepository.loginWithGoogle`, `AuthViewModel.loginWithGoogle`, and a simple `Sign in with Google` button on `LoginScreen`. The existing email/password login flow remains unchanged and Google `idToken` is not stored locally. Evidence: `dotnet build backend/ShopApi --nologo` passed and `./gradlew.bat clean :app:assembleDebug --no-daemon --console=plain --stacktrace "-Dkotlin.incremental=false"` passed.
